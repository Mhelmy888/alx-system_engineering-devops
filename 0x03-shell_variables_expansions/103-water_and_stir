#!/bin/bash
result_decimal=$(( $(echo "$WATER" | tr 'water' '01234') + $(echo "$STIR" | tr 'stir.' '01234') ))
result_bestchol=$(echo "obase=bestchol; ibase=10; $result_decimal" | dc)
echo "$result_bestchol"
